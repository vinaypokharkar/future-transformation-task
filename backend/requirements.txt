# Web framework
fastapi==0.139.1
uvicorn[standard]==0.51.0
python-multipart==0.0.32

# Config
pydantic-settings==2.14.2
# Required by pydantic's EmailStr, used to validate login payloads.
email-validator==2.3.0

# Database
sqlalchemy==2.0.51
pymysql==1.2.0
cryptography==49.0.0
alembic==1.18.5

# Auth
# pwdlib over passlib: passlib 1.7.4 (unmaintained since 2020) reads
# bcrypt.__about__.__version__, removed in bcrypt 4.1+. See ADR-006.
pwdlib[bcrypt]==0.3.0
# PyJWT over python-jose: jose is unmaintained and pulls in ecdsa,
# which carries the unfixed CVE-2024-23342 timing vulnerability. See ADR-006.
pyjwt==2.13.0

# AI / semantic search
sentence-transformers==5.6.0
faiss-cpu==1.14.3
pypdf==6.14.2

# Tests
pytest==9.0.1
httpx==0.28.1
